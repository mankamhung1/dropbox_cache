/*

function readFile() {
    if (this.files && this.files[0]) {
        var FR = new FileReader();
        FR.onload = function(event) {
           var fileToLoad = new Image();
           fileToLoad.addEventListener("load", function() {
           });
           fileToLoad.src = event.target.result;
        };
        FR.readAsDataURL(this.files[0]);
    }
}

document.getElementById("fileToLoad").addEventListener("click", readFile, false);

function clickLoad() {
  $('#fileToLoad').click();
}

*/
